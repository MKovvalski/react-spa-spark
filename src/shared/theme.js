// place all variables (colors, shadows, backgrounds) for your project here
import breakpoints from './breakpoints'

const colors = {
  primary: '#6699ff',
  secondary: '#00cc66',
  defaultBlack: '#000000'
}

const contentSize = {
  header: '34px',
  subHeader: '24px',
  list: '20px'
}

const fonts = {
  base: 'Helvetica Neue',
  secondary: 'sans-serif'
}
const theme = {
  breakpoints,
  colors,
  contentSize,
  fonts
}

export default theme
